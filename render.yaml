services:
  - type: web
    name: pixelcraft
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/pixelcraft/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: 20.x